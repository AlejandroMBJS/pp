node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
!.env.staging.example

# Next.js
.next/

# Build artifacts
*.tsbuildinfo

# Runtime data
data/

# Mac zip leftovers + test assets
__MACOSX/
Giraffe360*/
test_assets/
Unconfirmed*
Canteen*

# Stray binaries / large files
*.dwg
*.stl
*.3mf
*.glb
*.gltf
*.obj
*.zip
*.crdownload
*.pdf
*.dxf

# Secrets (moved to ~/.secrets-pp/)
cred
stripe
*.pem

# Editor / local scratchpads
.vscode/
.idea/
*.swp

# Claude Code runtime
.claude/scheduled_tasks.lock

# Go build artifacts (local)
backend/api

# Playwright / test artifacts
frontend/e2e/**/.artifacts/
frontend/playwright-report/
frontend/test-results/

# Per-host docker compose override (e.g. port mapping for a VPS without a
# host-level reverse proxy) — lives only on that specific VPS.
docker-compose.override.yml
